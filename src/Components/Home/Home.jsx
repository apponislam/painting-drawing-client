import { Helmet } from "react-helmet-async";

const Home = () => {
    return (
        <div>
            <Helmet>
                <title>Appon Printing & Drawing</title>
            </Helmet>
            <h1>This is home for</h1>
        </div>
    );
};

export default Home;
